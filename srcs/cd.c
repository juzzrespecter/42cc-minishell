#...

int cd (..., char **envp)
{
	/* func buscar var path dentro de envp */
	char *path = ..;

	/* func montar path destino */

	/* comprobar si existe con stat(), return si error */

	/* chdir */

	/* actualizar $PWD en envp */

	return (0);
}
